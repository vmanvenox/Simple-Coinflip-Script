import random

# Define the possible choices (heads and tails)
choices = ["heads", "tails"]

# Use the random.choice() function to choose one of the possible choices
choice = random.choice(choices)

# Print the result to the screen
print(choice)

# made by KingTreemox